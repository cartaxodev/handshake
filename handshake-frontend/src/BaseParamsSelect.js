import { useState } from "react";


function BaseParamsSelect(props) {

    const baseParamsObject = props.baseParamsObject;
    console.log(props);

    return (
        <select>

        </select>
    );
}

export default BaseParamsSelect;